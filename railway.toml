# Railway Deployment Configuration
# This file is optional but helps Railway understand your app better

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# CMD is handled in Dockerfile
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
